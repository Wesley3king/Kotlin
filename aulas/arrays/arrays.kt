

import java.util.Arrays

fun main(args: Array<String>) {
    var lista = arrayOf("Arroz", "feijão", "carne", "canudo frito")
    println(lista[2])
    println(Arrays.toString(lista))
    println("tamanho da lista: ${lista.size}")
}